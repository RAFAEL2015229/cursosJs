function tabuada() {
    let num = document.getElementById('numero1')
    let tab = document.getElementById('seltab')
    
    if(num.value.length == 0) {
        window.alert('Por favor, digie um numero!')
    } else {
        let n = Number(num.value)
    }
}